# @earendil-works/pi-web-tools

Public contracts and safety policy for vendor-neutral web operations. Adapters
remain responsible for provider integration; callers must validate URLs and
bound result payloads through the exported policy helpers.
