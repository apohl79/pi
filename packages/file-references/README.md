# @earendil-works/pi-file-references

Stable contracts for execution-host file references and bounded completion.
Filesystem authorization, canonicalization, reads, and uploads remain owned by
the server implementation.
