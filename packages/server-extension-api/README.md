# Pi server extension API

This package contains the public, transport-neutral contracts for server-runtime extensions. Implementations may register durable operation hooks, append-only state access, bounded request-only sampling contributors, and filtered runtime-event observers without importing the coding-agent host or TUI.
