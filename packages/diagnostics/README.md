# @earendil-works/pi-diagnostics

Portable contracts for correlated forensic events, integrity checks, encrypted
content capsules, diagnostic bundles, and server health providers. The server
package owns persistence and recorder implementations; this package defines
the stable data boundary.
